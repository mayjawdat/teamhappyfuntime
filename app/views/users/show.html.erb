<%= render 'contact_info' %>

<% if @profile.is_mentor %>
  <%= render 'skills' %>
<% end %>

<%= render 'my_appointments' if @profile.id == current_user.id %>

<%= render 'feedback_on_student' if (current_user.is_mentor && !@profile.is_mentor) %>

<%= render 'feedback_on_mentor' if (@profile.is_mentor && !@current_user.is_mentor) %>
